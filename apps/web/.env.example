VITE_API_BASE_URL=http://localhost:5080

# Google OAuth Client ID — create at https://console.cloud.google.com/apis/credentials
# (OAuth 2.0 Client, type "Web application", add http://localhost:5173 and your production URL as authorized JS origins)
# Public value, safe to commit to CI but keep out of git.
VITE_GOOGLE_CLIENT_ID=

