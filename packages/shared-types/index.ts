// Generated API types live in ./api.ts after running `pnpm --filter @medmanager/shared-types generate`.
// Run the API first (`pnpm api:dev`), then the codegen command.
// Re-export selected types here once generated so the web app imports them from "@medmanager/shared-types".

export {};
